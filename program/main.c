#include <stdio.h>

int main(void) {
  int a, b; // Declare two integer variables
  scanf("%d %d", &a, &b); // Read two integers from stdin
  printf("%d", a + b); // Print the sum of a and b
  return 0;
}
